name: chatting_app
description: A Flutter chat application with Firebase backend
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
    
  # Firebase
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  firebase_database: ^11.0.3
  firebase_storage: ^12.1.0
  
  # UI
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  
  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.5.3
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    # Removed empty assets/images directory; re-add if you introduce image assets
